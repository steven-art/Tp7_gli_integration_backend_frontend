import { FilterCardPipePipe } from './filter-card--pipe.pipe';

describe('FilterCardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
